# Serving-only deps (FastAPI + Streamlit + model inference + SHAP).
# Excludes mlflow, jupyter, matplotlib, seaborn, pytest (training/EDA only).
# imbalanced-learn is required: the saved pipeline is an imblearn Pipeline.
pandas>=2.2.3
numpy>=2.1.0
scikit-learn>=1.5.2
xgboost>=2.1.1
imbalanced-learn>=0.13.0
shap>=0.46.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
pydantic>=2.9.0
streamlit>=1.39.0
plotly>=5.24.0
requests>=2.32.3
joblib>=1.4.2
